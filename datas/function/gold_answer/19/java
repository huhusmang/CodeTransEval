public static int maxSubarrays(int[] nums) {
    int ans = 0;
    int a = -1; 
    for (int x : nums) {
        a &= x;
        if (a == 0) {
            ans++; 
            a = -1;
        }
    }
    return Math.max(ans, 1); 
}