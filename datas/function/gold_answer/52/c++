int pickingNumbers(const vector<int>& a) {
    unordered_map<int, int> d;
    for (int num : a) {
        ++d[num];
    }
    int best = 0;
    for (int i = 0; i < 99; ++i) {
        best = max(d[i] + d[i + 1], best);
    }
    return best;
}