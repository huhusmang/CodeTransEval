def countBeautifulPairs(nums: List[int]) -> int:
    ans, cnt = 0, [0] * 10
    for x in nums:
        for y in range(1, 10):
            if cnt[y] and gcd(x % 10, y) == 1:
                ans += cnt[y]
        while x >= 10: x //= 10
        cnt[x] += 1
    return ans