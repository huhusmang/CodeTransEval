def checkStrings(s1, s2):
    cnt1 = [[0] * 26, [0] * 26]
    cnt2 = [[0] * 26, [0] * 26]
    for i in range(len(s1)):
        cnt1[i % 2][ord(s1[i]) - ord('a')] += 1
        cnt2[i % 2][ord(s2[i]) - ord('a')] += 1
    return cnt1 == cnt2