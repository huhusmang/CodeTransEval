def divisorSubstrings(num: int, k: int) -> int:
    s = str(num)
    n = len(s)
    res = 0
    for i in range(n - k + 1):
        tmp = int(s[i:i+k])
        if tmp != 0 and num % tmp == 0:
            res += 1
    return res